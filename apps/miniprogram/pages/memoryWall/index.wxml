<view class="container">
  <!-- 加载中 -->
  <view wx:if="{{!ready}}" class="muted">加载中…</view>

  <!-- 未登录 -->
  <view wx:elif="{{!loggedIn}}" class="empty">
    <view class="empty-emoji">🔒</view>
    <view class="empty-title">请先登录</view>
    <view class="empty-hint">登录后即可查看你们的回忆墙～</view>
    <button class="btn ghost gate-btn" bindtap="goMe">去登录</button>
  </view>

  <!-- 未绑定情侣 -->
  <view wx:elif="{{!bound}}" class="empty">
    <view class="empty-emoji">💞</view>
    <view class="empty-title">还没有绑定另一半</view>
    <view class="empty-hint">先和 TA 绑定情侣关系，一起留下回忆吧～</view>
    <button class="btn gate-btn" bindtap="goMe">去绑定</button>
  </view>

  <block wx:else>
    <!-- 头部 -->
    <view class="hero">
      <view class="hero-title">回忆墙</view>
      <view class="hero-sub">收藏了你们的 {{memories.length}} 段回忆</view>
    </view>

    <!-- 立体回忆相册 -->
    <view wx:if="{{memories.length > 0}}" class="album">
      <swiper
        class="album-swiper"
        previous-margin="48rpx"
        next-margin="48rpx"
        circular="{{false}}"
        bindchange="onSwiperChange"
      >
        <swiper-item wx:for="{{memories}}" wx:key="id">
          <view
            class="mcard {{index === current ? 'mcard--active' : 'mcard--side'}}"
            bindtap="goDetail"
            data-id="{{item.id}}"
          >
            <!-- 封面 -->
            <view
              class="cover"
              catchtap="previewPhotos"
              data-photos="{{item.photos}}"
              data-cover="{{item.cover}}"
            >
              <image wx:if="{{item.cover}}" class="cover-img" src="{{item.cover}}" mode="aspectFill" />
              <view wx:else class="cover-ph">
                <text class="cover-ph-emoji">{{item.moodEmoji}}</text>
              </view>

              <!-- 多图角标 -->
              <view wx:if="{{item.photoCount > 1}}" class="photo-count">{{item.photoCount}} 张</view>

              <!-- 标题渐变带 -->
              <view class="cover-grad">
                <text class="cover-title">{{item.title || '未命名回忆'}}</text>
              </view>
            </view>

            <!-- 卡片正文 -->
            <view class="mbody">
              <view wx:if="{{item.content}}" class="mcontent">{{item.content}}</view>

              <view wx:if="{{item.tagList.length > 0}}" class="tags">
                <text wx:for="{{item.tagList}}" wx:for-item="tag" wx:key="*this" class="tag">{{tag}}</text>
              </view>

              <view class="mmeta">
                <text class="mmeta-date">{{item.dateText}}</text>
                <text wx:if="{{item.mood}}" class="mmeta-mood">{{item.moodEmoji}} {{item.mood}}</text>
              </view>
            </view>
          </view>
        </swiper-item>
      </swiper>

      <!-- 分页圆点 -->
      <view class="dots">
        <view
          wx:for="{{memories}}"
          wx:key="id"
          class="dot {{index === current ? 'dot--on' : ''}}"
        ></view>
      </view>
    </view>

    <!-- 空状态 -->
    <view wx:else class="empty">
      <view class="empty-emoji">📷</view>
      <view class="empty-title">还没有回忆</view>
      <view class="empty-hint">去地图写下第一段吧</view>
    </view>
  </block>
</view>
